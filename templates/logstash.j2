# {{ ansible_managed }}

###############################
# Default settings for logstash
###############################

# Override Java location
#JAVACMD=/usr/bin/java

# Set a home directory
LS_HOME={{ logstash_path_data }}

# Arguments to pass to java
LS_HEAP_SIZE="{{ logstash_java_heap_size }}"
LS_JAVA_OPTS="{{ logstash_java_opts }}"

# Logstash filter worker threads
#LS_WORKER_THREADS=1

# pidfiles aren't used for upstart; this is for sysv users.
#LS_PIDFILE=/var/run/logstash.pid

# user id to be invoked as; for upstart: edit /etc/init/logstash.conf
LS_USER={{ logstash_user }}

# logstash logging
LS_LOG_FILE={{ logstash_path_logs }}/logstash.log
#LS_USE_GC_LOGGING="true"

# logstash configuration directory
LS_CONF_DIR={{ logstash_path_conf }}

# Open file limit; cannot be overridden in upstart
LS_OPEN_FILES={{ logstash_nofile }}

# Nice level
#LS_NICE=0
